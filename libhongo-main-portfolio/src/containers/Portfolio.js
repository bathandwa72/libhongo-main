import React from "react";
import Navbar from "../components/Navbar";

const Portfolio = () => (
  <div className="portfolioContainer">
    <Navbar />
    <span className="portfolioTitle animated fadeIn slower">
      <h3>
        Sorry but the page is currently being updated.
        <br />
        Come back again soon to see the new content!
      </h3>
    </span>
  </div>
);

export default Portfolio;
